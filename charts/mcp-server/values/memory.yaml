# memory MCP server
# Reference knowledge-graph memory server (stdio transport).

image:
  repository: mcp/memory
  tag: latest
  pullPolicy: Always

# stdio-only server: no network endpoint; kept alive via stdin/tty.
stdio: true
containerPort: null

service:
  enabled: false
