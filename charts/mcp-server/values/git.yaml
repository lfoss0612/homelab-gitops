# git MCP server
# Reference git server (stdio transport).

image:
  repository: mcp/git
  tag: latest
  pullPolicy: Always

# stdio-only server: no network endpoint; kept alive via stdin/tty.
stdio: true
containerPort: null

service:
  enabled: false
