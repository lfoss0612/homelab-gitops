# python-refactoring MCP server
# Python refactoring MCP server (stdio transport).

image:
  repository: mcp/mcp-python-refactoring
  tag: latest
  pullPolicy: Always

# stdio-only server: no network endpoint; kept alive via stdin/tty.
stdio: true
containerPort: null

service:
  enabled: false
