apiVersion: v2
name: vllm
description: vLLM OpenAI-compatible inference server serving a single model on the cluster GPU, with a Longhorn-backed HuggingFace cache so models survive pod restarts.
type: application
version: 0.1.0
appVersion: "v0.25.1"
